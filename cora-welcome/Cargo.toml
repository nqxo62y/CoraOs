[package]
name = "cora-welcome"
version = "0.1.0"
edition = "2021"
description = "Console admin menu for CoraOS"

[dependencies]
sysinfo = "0.30"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
